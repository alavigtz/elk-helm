input {
    beats {
        port => "5044"
    }
}

output {
    elasticsearch {
        hosts => ["http://158.98.6.18:31996"]
        index => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => rubydebug
    }
}