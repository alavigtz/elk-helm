input {
    beats {
        port => "5044"
    }
}

output {
    elasticsearch {
        hosts => ["https://172.16.16.101:9200"]
        index => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => rubydebug
    }
}