input {
    beats {
        port => "5044"
    }
}

output {
    elasticsearch {
        hosts => ["http://172.16.16.101:31996"]
        index => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => rubydebug
    }
}